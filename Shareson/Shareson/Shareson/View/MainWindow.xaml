<Window x:Class="Shareson.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:Views="clr-namespace:Shareson.View.ViewControls"
        xmlns:ViewModels="clr-namespace:Shareson.ViewModel"
        Title="Shareson" Height="700" Width="1024" 
        WindowStyle="None" ResizeMode="NoResize">
        <!--AllowsTransparency="True" Background="Transparent"  ResizeMode="NoResize" -->
        <!--SizeToContent="WidthAndHeight"-->

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/CustomControls/Button1.xaml"/>
                <ResourceDictionary Source="/CustomControls/Button2.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <DataTemplate DataType="{x:Type ViewModels:ImagesControlViewModel}">
                <Views:ImagesControl/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type ViewModels:OptionsControlViewModel}">
                <Views:OptionsControl/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type ViewModels:ContactControlViewModel}">
                <Views:ContactControl/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type ViewModels:AccountOptionsControlViewModel}">
                <Views:AccountOptionsControl/>
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>

        <StackPanel Background="#003B53" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="4" Orientation="Horizontal" UseLayoutRounding="True">
            <Button Content="Images"  Command="{Binding CallImagesPage,  Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Button1}" Width="120" Margin="20,0,0,0"/>
            <Button Content="Options" Command="{Binding CallOptionsPage, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Button1}" Width="120" Margin="20,0,0,0"/>
            <Button Content="Contact" Command="{Binding CallContactPage, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Button1}" Width="120" Margin="20,0,0,0"/>
            <Button Content="Account" Command="{Binding CallAccountOptionsPage, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource Button1}" Width="120" Margin="20,0,0,0"/>
            <TextBlock Text="{Binding AccountStatus}" Width="120" Margin="100,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" Foreground="White"/>
        </StackPanel>
        <StackPanel Grid.Row="1" UseLayoutRounding="True">
            <ContentControl Content="{Binding MainWindowContentControl, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        <StackPanel Background="#003B53" Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="4" Orientation="Horizontal" UseLayoutRounding="True"/>

    </Grid>
</Window>
